<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <title>URL Shortener</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <header>
        <h3>URL Shortener</h3> 
    </header>

  <div class="container">
      <form action="/shortUrls" method="POST">
      <input required placeholder="enter link" type="url" name="originalUrl" id="originalUrl">
      <button type="submit">Shorten</button>
    </form> 
     
    <table  class="table table-bordered table-sm">
      <thead>
        <tr>
          <th>Original URL</th>
          <th>Short URL</th>
          <th>Clicks</th>
        </tr>
      </thead>
      <tbody>
        <% shortUrls.forEach(shortUrl => { %>
          <tr>
            <td><a href="<%= shortUrl.originalUrl %>"><%= shortUrl.originalUrl %></a></td>
            <td><a href="<%= shortUrl.shortUrl %>"><%= shortUrl.shortUrl %></a></td>
            <td><%= shortUrl.clicks %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  
  <footer>
    <span>developed by myriemsl.dev</span>
  </footer>
</body>
</html>